// You can use attachFields directly if you'd prefer to call it in your
// project's `gatsby-node.js` instead of using it as a plugin.

// eslint-disable-next-line import/prefer-default-export
export { attachFields } from './internal/attachFields'
